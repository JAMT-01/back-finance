# =============================================
# Mercado Pago Email Parser - Environment
# =============================================
# Copy to .env and fill in your values

# PostgreSQL Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/mercadopago_parser

# Server
PORT=3000

# JWT Authentication
JWT_SECRET=change-this-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d

# Webhook Security (must match Cloudflare Worker)
WEBHOOK_SECRET=super_secret_password

# Your email domain (configured in Cloudflare)
EMAIL_DOMAIN=jamty.xyz

# Frontend URL for CORS (optional)
FRONTEND_URL=http://localhost:3001
